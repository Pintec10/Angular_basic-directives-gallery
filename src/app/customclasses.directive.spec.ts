import { CustomclassesDirective } from './customclasses.directive';

describe('CustomclassesDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomclassesDirective();
    expect(directive).toBeTruthy();
  });
});
